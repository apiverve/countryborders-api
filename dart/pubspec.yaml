name: apiverve_countryborders
description: Country Borders is a simple tool for getting the list of neighboring countries for a specific country. It returns the list of neighboring countries for the specified country.
version: 1.1.14
homepage: https://countryborders.apiverve.com?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/countryborders-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
